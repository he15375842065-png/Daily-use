name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 0 */5 * *'  # 每5天执行一次

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: curl https://cqhmzlcmgtwxomubdzsv.supabase.co
